{
  "name": "product-service",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "lint": "eslint",
    "start": "NODE_ENV=test node server.js",
    "start:postgres": "NODE_ENV=test REPOSITORY_TYPE=postgres DB_HOST=localhost DB_PORT=5432 DB_USER=postgres DB_PASSWORD=postgres DB_NAME=product_service node server.js",
    "db:start": "docker compose up -d",
    "db:stop": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d",
    "state-server:start": "node ./automation/setup-postgres-state-http.js server",
    "state-server:stop": "lsof -ti:9000 | xargs kill -9 2>/dev/null || true",
    "test2": "NODE_ENV=test npx jest --colors --testTimeout 30000 --testMatch \"**/api-inmemory.test.js\" && NODE_ENV=test npx jest --colors --testTimeout 30000 --testMatch \"**/api-postgres.test.js\"",
    "test": "npm run test:inmemory && npm run test:postgres",
    "test:postgres": "NODE_ENV=test npm run db:start && npm run state-server:start & npx jest --colors --testTimeout 30000 --testMatch \"**/*-postgres.test.js\" && npm run state-server:stop",
    "test:inmemory": "NODE_ENV=test npx jest --colors --testTimeout 30000 --testMatch \"**/api-inmemory.test.js\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@pact-foundation/pact": "^16.3.0",
    "@pact-foundation/pact-cli": "^18.0.0",
    "@pactflow/drift": "^2603.0.4-beta.17",
    "dotenv": "^17.3.1",
    "eslint": "^10.0.3",
    "globals": "^17.4.0",
    "jest": "^30.3.0"
  },
  "license": "ISC"
}
